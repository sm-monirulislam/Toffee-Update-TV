name: "🎬 Toffee Auto Playlist Builder"

on:
  schedule:
    - cron: "0 */6 * * *"    # প্রতি ৬ ঘণ্টায় একবার রান
  workflow_dispatch:         # চাইলে ম্যানুয়ালি চালানো যাবে

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.x"

      - name: Install dependencies
        run: pip install requests

      - name: Run Toffee Script
        run: python toffee_play.py

      - name: Commit generated playlist
        run: |
          git config --global user.name "github-actions"
          git config --global user.email "github-actions@users.noreply.github.com"
          git add toffee_playlist.m3u
          git commit -m "🎬 Auto-update Toffee Playlist" || echo "No changes to commit"
          git push
